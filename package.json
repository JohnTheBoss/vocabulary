{
  "name": "frontend-build",
  "engines": {
    "node": "14"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "deploy": "cp -a frontend/dist/. backend/public/frontend",
    "heroku-postbuild": "npm run build && npm run deploy"
  }
}
